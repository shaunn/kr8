local apptio = import 'apptio.libsonnet';  // some helper functions
local config = std.extVar('kr8');

[
//  apptio.BindPSPToServiceAccount('akp:psp:clamav', config.serviceAccount, config.namespace, 'akp:psp:%s' % [config.release_name]),
]
